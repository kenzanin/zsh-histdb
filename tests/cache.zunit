#!/usr/bin/env zunit

@describe '_histdb_cache (stub — cache not yet implemented)'

@test 'returns empty for missing cache function' {
    local result
    result=$(_histdb_cache_get nonexistent 2>/dev/null) || true
    assert "$result" is_empty
}
