### Scratchpad — 2026-07-28T05:40:19.170Z

- Migrated fzf → peco for history TUI
  - Renamed `libsql-history-fzf.zsh` → `libsql-history-peco.zsh`
  - Renamed functions: `histdb-fzf` → `histdb-peco`, `histdb-fzf-tmux` → `histdb-peco-tmux`
  - Renamed shared extraction functions: `_histdb_fzf_extract_cmd` → `_histdb_peco_extract_cmd`, `_histdb_fzf_extract_field` → `_histdb_peco_extract_field`
  - Replaced fzf invocations with `peco --layout=bottom-up --query "$LBUFFER"` — metadata formatted inline since peco lacks --preview, --with-nth, --delimiter
  - Dropped features: preview window, Ctrl-J directory picker, Ctrl-K delete, Ctrl-R re-search (all depended on peco-unsupported `--expect` and `--preview`)
  - Updated: README.md, AGENT.md, EXPLORE.md, functions/histdb-info, zsh-histdb.plugin.zsh
  - Deleted: `libsql-history-fzf.zsh` + its `.zwc` compiled bytecode

